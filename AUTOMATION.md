# OO라이브 자동화 운영안

## 기본 흐름

1. 기능별 브랜치에서 개발합니다.
2. 변경 사항을 GitHub에 올리면 코드 검사와 프로덕션 빌드가 자동 실행됩니다.
3. 검사가 통과한 변경만 `main`에 합칩니다.
4. `main`의 검증된 버전을 Sites에 배포합니다.
5. 배포 성공 또는 실패 결과를 Slack 개인 운영 채널로 알립니다.

## 권장 브랜치 규칙

- `main`: 언제든 배포할 수 있는 안정 버전
- `codex/기능명`: 개발 중인 기능
- 직접 `main`에 큰 변경을 올리지 않고 Pull Request로 합치기

## 비밀값 관리

- 시세 API 키와 Slack 연결 정보는 `.env`나 호스팅 환경의 비밀값으로 관리합니다.
- GitHub 저장소에는 실제 키를 커밋하지 않습니다.
- 자동화 권한은 저장소 읽기부터 시작하고, 필요한 쓰기 권한만 추가합니다.

## 1인 운영 권장 알림

- 배포 실패
- 운영 사이트 응답 실패
- 일일 데이터 갱신 실패
- API 사용량 또는 비용 임계치 초과

정상 배포 알림은 선택 사항이며, 과도한 알림은 사용하지 않습니다.
