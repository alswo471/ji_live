# 카페형 커뮤니티·조회수·추천 구현

## 결과

기존 커뮤니티의 인증·신고·삭제·관리자·보존 정책을 유지하면서 목록을 제목 중심 게시판으로 정리했다. 데스크톱은 제목·작성자·작성일·조회·추천 열을 보여 주고, 모바일은 제목 아래에 메타정보를 묶는다. 특정 서비스의 로고·색상·자산을 복제하지 않고 지투라이브 테마와 기존 컴포넌트를 재사용했다.

`/community/write`는 기존 `PostForm`·익명 세션·Turnstile·재시도 idempotency key를 재사용한다. 성공하면 서버가 반환한 게시글 ID의 상세로 이동하고, 취소 동선과 페이지를 떠나면 초안이 사라진다는 안내를 표시한다.

## 집계 계약

- 조회: 검증된 actor·게시글 조합의 `last_counted_at`로 rolling 24시간을 적용한다. 23시간 59분에는 증가하지 않고 24시간 경계부터 다시 1회 반영한다.
- 추천: toggle이 아닌 `recommended: true | false`를 보내 재시도해도 같은 결과가 된다. 본인 글·비공개 글·삭제 글·활성 제재 사용자는 DB에서 거부한다.
- 동시성: 게시글 행을 먼저 잠그고 unique actor·post 관계를 갱신해 중복 증가와 음수를 막는다.
- 요청 제한: 조회는 60회/60초, 추천·취소는 합계 20회/60초를 actor와 daily HMAC에 각각 적용한다. 원본 IP는 저장하지 않는다.
- 권한: browser의 anon/authenticated role은 집계 table에 쓰거나 RPC를 실행할 수 없다. 서버가 JWT·Turnstile·trusted proxy 경계를 검증한 뒤 service role로만 호출한다.

## 오류와 보존

집계 migration이 없거나 집계 API가 실패해도 게시글 본문과 댓글 읽기는 계속한다. 알 수 없는 수치는 0으로 꾸미지 않고 `—`로 표시하며 상세에 재시도 동선을 제공한다. 조회 receipt는 마지막 반영 후 24시간이 지나면 hourly `pg_cron`이 정리하고, 누적 조회수는 유지한다. 추천 관계는 취소 시 즉시 삭제하고 사용자·게시글 실제 파기 시 cascade한다. 기존 삭제 콘텐츠 1년 보관과 legal hold는 변경하지 않았다.

브라우저 데이터 삭제나 새 기기로 새 익명 actor가 만들어지면 동일 사람을 완전히 판별할 수 없다. 이 문서는 법적 적합성을 보증하지 않는다.

## 검증과 배포 조건

초기 unit 기준선은 79 files, 541 passed, 6 skipped였다. 로컬 PostgreSQL에 additive migration을 적용한 뒤 rolling 경계, 동시 12요청, 멱등 추천·취소, 권한 거부, cleanup 스케줄을 검증했다. 외부 근군은 [조회수·추천 참고자료](../reference/2026-09-08_게시판_조회수_추천_참고자료.md)에 정리했다.

원격은 기존 `202609040001`~`202609070001`이 미적용이고 rate-limit RPC 반환형도 로컬과 다르다. 따라서 이 작업에서 원격 DB를 변경하지 않았다. [원격 DB 적용 상태](../operations/2026-09-08_원격_DB_적용상태.md)에 기록한 순서대로 DB migration을 먼저 검토·적용한 뒤 앱을 반영해야 한다.

## 리뷰에서 보완한 동시성 문제

DB 카운터가 정확하더라도 조회 응답이 추천 응답보다 늦게 도착하면 화면이 이전 스냅샷으로 되돌아갈 수 있었다. 처음에는 조회 처리 중 추천만 차단했으나, 최종 리뷰에서 추천 처리 중 조회 재시도라는 반대 경로도 확인했다. 공유 mutation 잠금을 두 handler에 적용해 양방향 재진입을 막았으며 UI도 처리 중인 상대 작업을 비활성화한다. 지연 응답 후 다음 클릭이 추천 취소(`recommended:false`)인지 회귀 테스트로 검증했다.

또한 실제 로컬 DB에서 추천자 삭제 후 남은 게시글 추천 수 감소, 게시글 파기 후 관계 cascade, 조회 receipt 정리 후 누적 조회수 유지를 검증했다. 원격 데이터로 삭제 테스트를 하지 않았다.

## 최종 검증 기록

2026-09-08 코드 commit `02692c3` 기준:

- `pnpm test`: 574 passed, 13 skipped (83 files passed, 1 skipped). 선택 실행하는 integration 항목의 skip을 통과로 계산하지 않았다.
- `RUN_LOCAL_SUPABASE_TESTS=true pnpm test tests/integration/community-engagement.test.ts`: 7 passed.
- `pnpm exec tsc --noEmit`, `pnpm lint`, `pnpm build`: 성공. 빌드는 plugin 소요시간 안내와 vinext의 route 분류 안내를 출력했으나 오류 없이 종료했다.
- 실제 브라우저: 목록·별도 작성 페이지·상세 읽기·집계 오류 표시와 모바일 390px 메타 배치를 확인했다. 원격 추천/작성 성공까지 검증한 것은 아니다.
- 별도 task review, 수정 재검토, 전체 branch review와 최종 수정 재검토를 마쳤다. 마지막 검토에서 열린 Critical/Important 항목은 없다.

구현 commit은 `113e0b5`, 응답 순서 수정은 `a6d725a`, 양방향 재진입 차단·cascade 검증 보완은 `02692c3`에 기록했다. 원격 DB 적용과 공유 브랜치 병합은 이 검증 완료와 별개다.
